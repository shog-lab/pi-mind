{
  "name": "pi-mind-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo housing pi-mind, pi-toolkit, and pi-chrome — agent capabilities for pi-coding-agent.",
  "type": "module",
  "license": "MIT",
  "workspaces": [
    "packages/memory",
    "packages/toolkit",
    "packages/chrome"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
