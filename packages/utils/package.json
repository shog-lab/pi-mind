{
  "name": "pi-utils",
  "version": "0.1.0",
  "private": true,
  "description": "Shared infrastructure for pi-mind packages: pi spawn helper, .pi-mind path resolution. Internal — kept private until consumers (memory/ralph/eval) need it on npm.",
  "type": "module",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist/**", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=20"
  }
}
