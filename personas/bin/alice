#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

PERSONA="alice"
MODEL="${ALICE_MODEL:-deepseek/deepseek-v4-pro}"
EXCLUDE_TOOLS="${ALICE_EXCLUDE_TOOLS:-}"
NO_SKILLS="${ALICE_NO_SKILLS:-}"

exec "$SCRIPT_DIR/run" "$PERSONA" "$MODEL" "$EXCLUDE_TOOLS" "$NO_SKILLS" "$@"
